<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="global.sesoc.jazart.dao.BoardMapper">
	<insert id="write" parameterType="Board">
		insert 
		into 
			board
			(
			 boardnum		
			,board_tag				
			,board_nickname
			,board_title
			,board_content			
			,board_inputdate	
			,board_hits			
			)
		values 
			(
				board_seq.nextval
				,#{board_tag}
				,#{board_nickname}
				,#{board_title}
				,#{board_content}
				,sysdate
				,0	
			)
	</insert>

	<select id="boardList" resultType="Board">
		select
			boardnum		
			,board_tag				
			,board_picture
			,board_nickname
			,board_title
			,board_content			
			,to_char(board_inputdate, 'yyyy-mm-dd') board_inputdate		
			,board_hits			
		from
			board	
		order by
			boardnum desc
	</select>
	
	<select id="selectList" parameterType="int" resultType="Board" >
		select
			boardnum		
			,board_tag				
			,title			
			,content			
			,to_char(inputdate, 'yyyy-mm-dd hh:mi') inputdate		
			,hits
			,originalfile
			,savedfile
		from
			board3
		where
			boardnum=#{boardnum}
	
	</select>
</mapper>	